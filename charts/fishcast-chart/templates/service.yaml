apiVersion: v1
kind: Service
metadata:
  name: {{ include "fishcast.fullname" . }}
  labels:
    {{- include "fishcast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fishcast.selectorLabels" . | nindent 4 }}
